---
description: Синхронизировать дашборд с Google-таблицей управленческой отчётности (накопление недель) и задеплоить в main
---

«Кнопка» обновления: подтянуть свежие даты из Google-таблицы управленческой отчётности, накопительно пересчитать эталон и вшить данные в дашборд, задеплоить. Только данные — дизайн/логику НЕ трогать.

Источник (Google Sheets, узум): `1E8K4jTkdWqXfxi5X-UEbPTl0laWsHLIEzYSN12OzSoE` — одна вкладка на дату (имя = ДД.ММ.ГГГГ), колонки `Адрес, ПВЗ, Дата, Кол-во заказов, Общая сумма`. Доступ только через Google Drive коннектор (`mcp__b6539439-18bf-4a04-bb92-98bbf8433116__*`), под аккаунтом пользователя — публично файл не открыт.

ПРЕДУСЛОВИЕ: `Эталон_ПВЗ.xlsx` должен быть ЗАКРЫТ в Excel (build_data его перезаписывает). `gsheet_sync.py` это проверяет и остановится с понятной ошибкой, если файл занят, — тогда попроси пользователя закрыть Excel и повтори.

1. **Скачай книгу как xlsx — через субагента** (Agent tool, general-purpose), чтобы base64 не засорял основной контекст. Промпт субагенту (дословно по смыслу):
   - Загрузить схему: ToolSearch `select:mcp__b6539439-18bf-4a04-bb92-98bbf8433116__download_file_content`.
   - Вызвать `download_file_content` с `fileId="1E8K4jTkdWqXfxi5X-UEbPTl0laWsHLIEzYSN12OzSoE"`, `exportMimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"`. В ответе поле `content` — base64.
   - Записать base64 в `C:\Users\Zephyrus\OneDrive\Project X\__gsheet.b64`, затем декодировать:
     `python -c "import base64; open(r'C:\Users\Zephyrus\OneDrive\Project X\__gsheet.xlsx','wb').write(base64.b64decode(open(r'C:\Users\Zephyrus\OneDrive\Project X\__gsheet.b64').read().strip()))"`
   - Проверить openpyxl: листы — даты ДД.ММ.ГГГГ; колонки `Адрес/ПВЗ/Дата/Кол-во заказов/Общая сумма`; собрать коды ПВЗ. Вернуть кратко: число листов, диапазон дат, любые НЕ-датовые листы, коды ПВЗ. Оставить `__gsheet.xlsx`, удалить `.b64`. Ничего не коммитить.
   - Если коннектор недоступен/структура иная — остановись, покажи и НЕ меняй данные.

2. **Синхронизация:**
   ```
   python gsheet_sync.py __gsheet.xlsx Эталон_ПВЗ.xlsx index.html
   ```
   Скрипт: покажет НОВЫЕ ДАТЫ (сверка с `.gsheet_ledger.json`), проверит что эталон не занят, прогонит `build_data.py` (накопление: полные недели не трогает, последнюю неполную дополняет, новые дописывает; правит ТОЛЬКО `Заказы_по_неделям`/`Объём_по_неделям`), запишет `DATA`/`WK` (+ накопит `SAL`) в `data.json`, обновит леджер. Покажи его вывод.
   - Если упал (занят эталон / не те колонки / нет даты-листов / нет ПВЗ в Справочнике) — остановись, покажи ошибку, руками данные НЕ правь. Если новый ПВЗ есть в таблице, но нет в `Справочник` — build_data его пропустит; сообщи пользователю (строку в Справочник добавляет он).

3. **Синхронизируй копию:** скопируй `index.html` → `Uzum_дашборд.html` (идентичны; сверь хэш).

4. **Проверки:** (а) `data.json` — валидный JSON с ключами `DATA`/`WK`/`FACT`/`SAL`, число ПВЗ (`DATA`) = Справочнику: `"C:\Program Files\nodejs\node.exe" -e "const d=JSON.parse(require('fs').readFileSync('data.json'));console.log(d.DATA.length, Object.keys(d))"`. (б) `node --check` на инлайн-`<script>` из `index.html`. Не прошло — стоп, покажи ошибку.

5. **Деплой (без вопросов, без PR):**
   ```
   git add -A
   git commit -m "sync: <новые/обновлённые недели>"
   git push origin main
   ```
   `__gsheet.xlsx` в коммит не попадёт (`.gitignore`: `__*`). `.gsheet_ledger.json` коммитится. Railway пересоберёт сайт.

6. **Подчисти и отчитайся:** удали `__gsheet.xlsx` (конфиденциальные дневные данные). Кратко: новые даты, обновлённая последняя неделя (дни было→стало), новые недели, run-rate-неделя, что задеплоено.
