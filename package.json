{
  "name": "uzum-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Uzum ПВЗ — интерактивный дашборд (статический сайт)",
  "scripts": {
    "start": "npx --yes serve . -c serve.json -l ${PORT:-3000}"
  }
}
