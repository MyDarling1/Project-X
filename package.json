{
  "name": "uzum-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Uzum ПВЗ — интерактивный дашборд (статический сайт)",
  "scripts": {
    "start": "node server.js",
    "checks": "echo 'Pre-commit (Windows): use full node path - \"C:\\Program Files\\nodejs\\node.exe\" --check + data.json validation per CLAUDE.md and HANDOVER'"
  }
}
